<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
  <title>{{ title }}</title>
  <link rel="stylesheet" href="/assets/css/styles.css" />
  </head>
  <body>
    <header>
      <nav>
        {{ for link of nav }}
          <a href="{{ link.url }}">{{ link.name }}</a>
        {{ /for }}
      </nav>
    </header>
    {{ content }}
  </body>
</html>
