---
title: Default
language: en
layout: layouts/main.vto
---

<article lang="{{ language }}">
  <h1>{{ title }}</h1>
  {{ content }}
  <div>Published: {{ date.toLocaleDateString('en-US', { month: 'long', year: 'numeric' }) }}</div>
</article>
